# Extra dependencies for the optional sarpy dashboard subpackage.
# These are intentionally kept separate from the core sarpy requirements,
# which remain numpy/scipy only.
fastapi>=0.100
uvicorn>=0.23
pillow>=9.0
